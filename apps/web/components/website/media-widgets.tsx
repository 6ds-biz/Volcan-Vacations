import type {Editorial} from '../../lib/website/registry';
import {Container,LinkButton} from '../ui';
import {ConfiguredMedia} from './public-media';
export function ImageText({config:c}:{config:Editorial}){return <section className={`section website-image-text website-image-${c.layout||'image-left'}`}><Container><div className="website-image-text-grid">{c.media&&<div className="website-image-text-visual"><ConfiguredMedia config={c.media}/></div>}<div>{c.eyebrow&&<p className="eyebrow">{c.eyebrow}</p>}{c.heading&&<h2>{c.heading}</h2>}{c.subheadline&&<h3>{c.subheadline}</h3>}{c.copy&&<p>{c.copy}</p>}{c.href&&<LinkButton href={c.href} arrow>{c.label||'Explore'}</LinkButton>}</div></div></Container></section>;}
export function FullWidthMedia({config:c}:{config:Editorial}){return <section className={`website-full-media website-height-${c.height||'medium'}`}>{c.media&&<ConfiguredMedia config={c.media} scenic/>}<Container><div>{c.eyebrow&&<p className="eyebrow">{c.eyebrow}</p>}{c.heading&&<h2>{c.heading}</h2>}{c.subheadline&&<h3>{c.subheadline}</h3>}{c.copy&&<p>{c.copy}</p>}{c.href&&<LinkButton href={c.href} arrow>{c.label||'Explore'}</LinkButton>}</div></Container></section>;}
